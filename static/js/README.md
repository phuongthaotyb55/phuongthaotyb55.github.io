Reserved for a future Pyodide-powered interactive demo on the temporal-graphs
project page (`#demo-root` in `templates/project_temporal_graphs.html`), letting
visitors run `greedy_dijkstras.py` live in the browser via WebAssembly. Not built
yet — client-side only, so it stays compatible with GitHub Pages' static hosting.
